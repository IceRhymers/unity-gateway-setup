# Python dependencies for the config generator (agent_setups/scripts).
#
# There are none. Every generator (claude-code, claude-desktop, codex, dsh) uses
# the standard library only.
#
# `tomlkit` used to live here for the `mcp` subcommand's Codex config.toml merge,
# which round-tripped the TOML document so a user's comments and formatting
# survived an in-place merge. That subcommand is gone — `ug mcp add` registers MCP
# servers now — so the dependency went with it.
